package jipplugin;

import org.eclipse.core.runtime.IStatus;

public class 
ActivatorLog 
{
	public static IStatus 
	logMessage
	(IStatus status)
	{
		Activator.getDefault().getLog().log(status);
		return status;
	}

}
