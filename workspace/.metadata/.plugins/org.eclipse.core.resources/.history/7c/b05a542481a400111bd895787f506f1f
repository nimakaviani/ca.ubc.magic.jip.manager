package jipplugin;

import org.eclipse.core.runtime.IStatus;

public class 
ActivatorLog 
{
	public static void 
	logMessage
	(IStatus status)
	{
		Activator.getDefault().getLog().log(status);
	}
}
